
// Fichier complètement vide pour éviter tout problème de processus

// Plus aucune gestion de processus
// Le build se terminera naturellement sans intervention

export function forceProcessExit() {
  // Fonction vide
}

export function addCleanupHandler() {
  // Fonction vide
}

export function removeCleanupHandler() {
  // Fonction vide
}
